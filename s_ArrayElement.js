// Push adds elements to the end of the array
var nums = [2,3,4,5];
print(nums);
// nums.push(1);
// print("Push adds elements to the end of the array");
// print(nums);

// print("\n")

// // Unshift adds elements to the begining of the array
// var newnum = [2,3,4,5];
// print(newnum);
// newnum.unshift(0,1)
// print("Unshift adds elements to the begining of the array");
// print(newnum);

// print("\n")
// print("pop is used to remove element at the end of an array");
// newnum.pop();
// print(newnum);

// print("\n")
// print("shift is used to remove element at the begining of an array");
// nums.shift();
// print(nums);

// Length can be use to add elements at the end of an array
// nums[nums.length] = 8;
// print(nums)

// Adding elements at the end of an array without js function
// var N = nums.length;
// for (let i = 0; i >= N; --i) {
//     nums[i] = nums[i-1];
// }
// nums[0] = 12;
// print(nums)

